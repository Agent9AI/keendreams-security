# Dependencies
node_modules/

# Local settings with real values. Only .dev.vars.example is committed.
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*

# Cloudflare and build output
.wrangler/
dist/
coverage/

# OS and editor
.DS_Store
*.log

# Local deploy overlay: carries this deployer's own resource ids. Never committed.
wrangler.deploy.jsonc
